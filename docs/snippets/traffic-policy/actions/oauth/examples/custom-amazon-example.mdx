

<CodeGroup>
```yaml policy.yml highlight={4-10}
on_http_request:
  - actions:
    - type: "oauth"
    config:
    provider: "amazon"
    client_id: "{your app's oauth client id}"
    client_secret: "{your app's oauth client secret}"
    scopes:
    - "profile"
```
```json policy.json highlight={5-15}
{
  "on_http_request": [
    {
      "actions": [
        {
          "type": "oauth",
          "config": {
            "provider": "amazon",
            "client_id": "{your app's oauth client id}",
            "client_secret": "{your app's oauth client secret}",
            "scopes": [
              "profile"
            ]
          }
        }
      ]
    }
  ]
}
```
</CodeGroup>
