/**
 * This component is a placeholder for the TabItem component.
 * See ./index.tsx for why we've structured it this way.
 */
export default function TabItem(props: any) {
  return <div {...props}>{props.children}</div>;
}
