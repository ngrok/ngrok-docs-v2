---
pagination_next: integrations/1password/index
sidebar_position: 1
---

import { json } from "@remix-run/node";
import { useLoaderData } from "@remix-run/react";
import IntegrationsList from "@components/IntegrationsList";

export const loader = async () => {
  return {
    integrationList: await getIntegrationList(),
  };
};

export function List() {
  const { integrationList } = useLoaderData();
  return <IntegrationsList list={integrationList} />;
}

# Integrations Hub

<List />
